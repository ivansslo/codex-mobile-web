{
  "name": "codex-mobile-web",
  "version": "0.1.11",
  "description": "A local authenticated mobile web client for Codex app-server.",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node server.js",
    "test": "node --test",
    "check": "node --check server.js",
    "deploy:cf": "wrangler deploy",
    "dev:cf": "wrangler dev",
    "build": "echo 'No build step required for static assets'",
    "check:macos": "echo 'check:macos skipped on non-macOS CI'"
  },
  "keywords": [
    "codex",
    "mobile",
    "web",
    "app-server",
    "tailscale"
  ],
  "dependencies": {
    "web-push": "^3.6.7",
    "@cloudflare/kv-assetstore": "^2.0.0"
  },
  "devDependencies": {
    "@cloudflare/wrangler": "^3.0.0"
  }
}
