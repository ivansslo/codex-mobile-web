{
  "name": "codex-mobile-web",
  "version": "0.1.11",
  "description": "A local authenticated mobile web client for Codex app-server.",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node server.js",
    "test": "node --test",
    "check": "node --check server.js",
    "deploy:cf": "wrangler deploy",
    "dev:cf": "wrangler dev",
    "build": "echo 'No build step required for static assets'",
    "check:linux-android-arm64": "echo 'check:linux-android-arm64 skipped on non-ARM64 CI'"
  },
  "keywords": [
    "codex",
    "mobile",
    "web",
    "app-server",
    "tailscale"
  ],
  "dependencies": {
    "web-push": "^3.6.7"
  },
  "devDependencies": {}
}
